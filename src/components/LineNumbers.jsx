import React from "react";

// function createLineNumbers() {
// 	let lineNumbers = [];

// 	for (let i = 1; i < 1000; i++) {
// 		lineNumbers.push(i);
// 	}

// 	return lineNumbers; 
// }

export default () => {
  return (
    <div className="line-numbers unselectable">
      {/* {createLineNumbers().map( num => <p>{num}</p>)} */}
    </div>
  );
}